% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/prepare-certify-simplex.R
\name{ripr_certify_simplex_data}
\alias{ripr_certify_simplex_data}
\title{Certification payload: the branch-and-bound record}
\usage{
ripr_certify_simplex_data(nodes, tol = NULL)
}
\arguments{
\item{nodes}{The data frame returned by \code{ripr::certify_trace()}, carrying
per-iteration bounds in its \code{"trace"} attribute, incumbents in
\code{"incumbent_trace"} and the certificate in \code{"certificate"} -- \strong{or} a
plain list with elements \code{nodes} (columns \code{part}, \code{cell}, \code{id}, \code{parent},
\code{depth}, \code{born}, \code{retired}, \code{fate}, \code{upper}, \code{volume}, \code{vertices}),
\code{trace}, \code{incumbent_trace} and \code{certificate} of the same shapes.}

\item{tol}{The tolerance the search was run with, echoed into the payload's
certificate for display. Defaults to the certificate's own \code{tol} when it
carries one.}
}
\value{
A list with elements \code{cells} (one \code{list(part, vertices)} per cell,
the vertices being the cell's root region), \code{nodes} (the node table as
parallel arrays), \code{upper}/\code{lower} (per-cell bound and incumbent traces)
and \code{certificate}, shaped for \code{\link[=ripr_certify_simplex]{ripr_certify_simplex()}}.
}
\description{
Packages the node table and per-iteration traces recorded by
\code{ripr::certify_trace()} for \code{\link[=ripr_certify_simplex]{ripr_certify_simplex()}}. The search runs one
branch-and-bound tree per \emph{cell} (a triangulated piece of a declared part
of the null; a part that is already a simplex is its own single cell), so
the node ids, traces and enclosure windows here are all per cell, with
\code{part} retained to say which declared part each cell came from.
}
