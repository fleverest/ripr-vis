% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/widget-compare.R
\name{ripr_compare}
\alias{ripr_compare}
\title{Comparing fits: KL, gap and growth against step or clock time}
\usage{
ripr_compare(compare, width = NULL, height = NULL, elementId = NULL)
}
\arguments{
\item{compare}{The payload from \code{\link[=ripr_compare_data]{ripr_compare_data()}}.}

\item{width, height, elementId}{Standard \code{\link[htmlwidgets:createWidget]{htmlwidgets::createWidget()}}
arguments.}
}
\value{
An htmlwidget.
}
\description{
Three stacked panels -- KL divergence, duality gap (log scale) and the
guaranteed growth rate \eqn{KL - \log(1 + gap)} -- with one line per
run, so two fitting algorithms, or two schedules of the same one, can be
read against each other. Tabs switch the horizontal axis between the step
count and the clock time \code{ripr} records in the trace; a toggle puts that
axis on a log scale. Hovering reads every run off at the cursor.
}
