% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/widget-fit.R
\name{ripr_fit}
\alias{ripr_fit}
\title{Fitting the projection: the Bernstein field and its diagnostics}
\usage{
ripr_fit(problem, fit, lattice, width = NULL, height = NULL, elementId = NULL)
}
\arguments{
\item{problem}{The payload from \code{\link[=ripr_problem_data]{ripr_problem_data()}}.}

\item{fit}{The payload from \code{\link[=ripr_fit_data]{ripr_fit_data()}}.}

\item{lattice}{The payload from \code{\link[=ripr_lattice_data]{ripr_lattice_data()}}; the widget rebuilds
the Bernstein basis from it in the browser.}

\item{width, height, elementId}{Standard \code{\link[htmlwidgets:createWidget]{htmlwidgets::createWidget()}}
arguments.}
}
\value{
An htmlwidget.
}
\description{
An animated view of a RIPr fit. The left panel draws the field
\eqn{G_i(\theta) = E_\theta[Q/P_i]} as filled contour bands over the
simplex -- red where it exceeds one, with the \eqn{G = 1} level set drawn
solid -- together with the support of the current iterate and the point
where the duality gap is attained. The right panels trace KL divergence,
gap, and the guaranteed log-growth rate \eqn{KL - \log(1 + gap)} across the
fit, with the current step picked out. A step slider and play toggle drive
the animation.
}
