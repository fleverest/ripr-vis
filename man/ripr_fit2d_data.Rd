% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/prepare-fit2d.R
\name{ripr_fit2d_data}
\alias{ripr_fit2d_data}
\title{Planar fit payload: the field of a RIPr fit over a grid of means}
\usage{
ripr_fit2d_data(state, problem, sigma = NULL, nx = 112L)
}
\arguments{
\item{state}{A \code{ripr} fit state (the result of \code{ripr::ripr_init()} with a
quadrature engine, advanced by \code{ripr::fw_step()}/\code{ripr::em_step()}),
\strong{or} a plain list with elements \code{trace} and \code{snapshots} as in
\code{\link[=ripr_fit_data]{ripr_fit_data()}} plus an \code{engine} element \verb{list(nodes = <M x 2 matrix>, log_w = <length-M vector>)} of quadrature nodes and log
weights.}

\item{problem}{The payload from \code{\link[=ripr_problem2d_data]{ripr_problem2d_data()}}; fixes the grid
extent.}

\item{sigma}{The family's known covariance. Defaults to the covariance of
\code{state}'s own family when \code{state} is a \code{ripr} object, else the identity.}

\item{nx}{Grid columns; rows follow from the extent's aspect ratio.}
}
\value{
A list with elements \code{field} (the grid: \code{nx}, \code{ny}, \code{x0}, \code{y0},
\code{dx}, \code{dy}), \code{z} (one length-\code{nx*ny} vector of \code{log10} field values per
snapshot, row-major from the bottom-left), and the usual \code{support},
\code{kl}, \code{gap}, \code{gap_theta}, \code{phase} diagnostics, shaped for
\code{\link[=ripr_fit2d]{ripr_fit2d()}}.
}
\description{
The planar analogue of \code{\link[=ripr_fit_data]{ripr_fit_data()}}, for two-dimensional families
such as \code{ripr::gaussian_family()}. On the simplex the field
\eqn{G_i(\theta) = E_\theta[Q/P_i]} crosses to the browser as Bernstein
coefficients; a continuous family has no such finite basis, so this helper
evaluates the field in R over the plotting grid, one grid of values per
recorded snapshot, using the same identity and the same quadrature the fit
itself ran on: \eqn{G(\theta) = E_Q[P_\theta/P_i]}, an importance-weighted
sum over the engine's frozen nodes. The gap markers in the trace therefore
agree with the drawn field by construction.
}
\details{
The grid covers \code{problem$extent} (see \code{\link[=ripr_problem2d_data]{ripr_problem2d_data()}}) with \code{nx}
columns and square cells, so the field is computed exactly where the
widget will draw.
}
