% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/prepare-fit-simplex.R
\name{ripr_fit_simplex_data}
\alias{ripr_fit_simplex_data}
\title{Fit payload: the trace of a RIPr fit, one field per step}
\usage{
ripr_fit_simplex_data(state, lattice, q = NULL, weights = NULL, q_pmf = NULL)
}
\arguments{
\item{state}{A \code{ripr} fit state (the result of \code{ripr::ripr_init()} advanced
by \code{ripr::fw_step()}/\code{ripr::em_step()}), \strong{or} a plain list with
elements \code{trace} (a data frame with columns \code{fw}, \code{lb}, \code{em}, \code{weight},
\code{phase}, \code{kl}, \code{gap} and a \code{gap_theta} list column) and \code{snapshots} (a
list of \code{list(iters, phase, atoms, weights)} as \code{ripr} records them).}

\item{lattice}{The lattice payload from \code{\link[=ripr_lattice_data]{ripr_lattice_data()}}.}

\item{q, weights}{The alternative, as in \code{\link[=ripr_problem_simplex_data]{ripr_problem_simplex_data()}}: a
categories-by-atoms matrix (or vector) and mixture weights over its
columns. Used to evaluate \code{Q}'s pmf over the lattice. Alternatively pass
\code{q_pmf} directly and leave these \code{NULL}.}

\item{q_pmf}{\code{Q}'s pmf over the lattice outcomes, if already computed.}
}
\value{
A list with elements \code{ratio}, \code{support}, \code{kl}, \code{gap}, \code{gap_theta}
and \code{phase}, one entry per snapshot, shaped for \code{\link[=ripr_fit_simplex]{ripr_fit_simplex()}}.
}
\description{
\eqn{G_i(\theta) = E_\theta[Q/P_i]} is a polynomial in \eqn{\theta} whose
Bernstein coefficients on the simplex are exactly the pointwise ratios
\eqn{Q(y)/P_i(y)}, so that vector is all the widget needs to draw the field
at step \eqn{i}. This helper walks the fit's snapshots, computes the ratio
vector and support of every recorded iterate, and lines each up with the
\code{kl}/\code{gap} diagnostics on the matching trace row.
}
\details{
The fit must have been run with \code{ripr::ripr_control(snapshot = "all")} (or
\code{"step"}), and \code{record_gap = TRUE} on the steps whose gap should appear.
}
