% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/prepare-problem2d.R
\name{ripr_problem2d_data}
\alias{ripr_problem2d_data}
\title{Planar problem payload: polyhedral null and alternative mean}
\usage{
ripr_problem2d_data(
  null,
  q,
  weights = NULL,
  title = "ripr",
  part_labels = NULL,
  margin = 0.2
)
}
\arguments{
\item{null}{A \code{ripr::null_model()}, a list of \code{ripr} convex regions, \strong{or}
a plain list with one element per part, each either a \verb{(2, nv)} vertex
matrix or a \verb{list(v = <2 x nv matrix>, r = <2 x nr matrix or NULL>, l = <2 x nl matrix or NULL>)} of vertices, rays and lineality
directions.}

\item{q}{The alternative's support: a \verb{(2, J)} matrix (or a single length-2
vector) of parameter points.}

\item{weights}{Mixture weights over the columns of \code{q}; equal by default.}

\item{title}{Short name for the example, used to namespace SVG defs.}

\item{part_labels}{Optional character vector labelling the parts.}

\item{margin}{Fraction of the vertex bounding box's larger span added as
padding on every side of the plot.}
}
\value{
A list with elements \code{seeds} (per-part \code{v}/\code{r}/\code{l} generator
lists), \code{marks}, \code{labels} and \code{extent} (\verb{$x} and \verb{$y} ranges), shaped
for \code{\link[=ripr_problem2d]{ripr_problem2d()}} and \code{\link[=ripr_fit2d]{ripr_fit2d()}}.
}
\description{
The planar analogue of \code{\link[=ripr_problem_simplex_data]{ripr_problem_simplex_data()}}, for two-dimensional
families whose parameter is a point of the plane (e.g. the mean of a
\code{ripr::gaussian_family()}). The null's parts are convex polyhedra given in
V-representation -- vertices plus optional recession rays and lineality
directions -- so unbounded parts draw correctly, clipped to the viewport.
}
\details{
The payload also fixes the plotting extent: the bounding box of every
part's vertices and the alternative's atoms, padded on all sides by
\code{margin} times the box's larger span. Rays recede to the viewport edge,
so the vertices are what the eye needs framed; sizing to them keeps the
interesting geometry -- the pointy ends nearest the alternative -- filling
the view however far the null extends.
}
\examples{
ripr_problem2d_data(
  null = list(list(
    v = cbind(c(1, 0), c(2, 0.5), c(2, -0.5)),
    r = cbind(c(1, 0))
  )),
  q = c(0, 0)
)
}
