% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/prepare-problem.R
\name{ripr_problem_data}
\alias{ripr_problem_data}
\title{Problem payload: the null region and the alternative's support}
\usage{
ripr_problem_data(null, q, weights = NULL, title = "ripr", part_labels = NULL)
}
\arguments{
\item{null}{A \code{ripr::null_model()}, a list of \code{ripr} convex regions, \strong{or}
a plain list of vertex matrices (one per part, categories by vertices --
each column a point of the simplex).}

\item{q}{The alternative's support: a categories-by-atoms matrix (or a
single vector) of points of the simplex.}

\item{weights}{Mixture weights over the columns of \code{q}; equal by default.}

\item{title}{Short name for the example, used to namespace SVG defs when
several widgets share a page.}

\item{part_labels}{Optional character vector labelling the parts of the
null, e.g. the inequality each represents.}
}
\value{
A list with elements \code{seeds} (per-part vertex lists), \code{marks} (the
alternative's atoms and weights) and \code{labels}.
}
\description{
Packages the geometry every widget draws for context: the convex parts
whose union is the null hypothesis, and the support of the alternative \code{Q}.
All three widgets (\code{\link[=ripr_problem]{ripr_problem()}}, \code{\link[=ripr_fit]{ripr_fit()}}, \code{\link[=ripr_certify]{ripr_certify()}}) take
this as their first argument.
}
\examples{
ripr_problem_data(
  null = list(cbind(c(.5, .5, 0), c(0, 1, 0), c(0, 0, 1))),
  q = c(0.4, 0.34, 0.26),
  part_labels = "example part"
)
}
